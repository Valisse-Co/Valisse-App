## Smart Service Match settings visual check

- The master Smart Service Match toggle renders with its white thumb contained inside the track and positioned on the left when off.
- The per-service controls are hidden behind the optional Customize services disclosure, keeping the initial Settings screen uncluttered.
- The inspected preview account currently has no configured services in this Settings data context, so the expanded disclosure correctly presents the Manage services empty state.
