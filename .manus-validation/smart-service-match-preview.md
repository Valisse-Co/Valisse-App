# Smart Service Match Settings Preview Validation

The dedicated Settings page renders the master Smart Service Match control and the collapsed Customize services section correctly in the active preview. The current test account has no persisted technician-service rows, so it shows the intentional empty-service state until services are configured. Per-service questionnaire and decision-path previews are available after the technician has at least one configured service.
